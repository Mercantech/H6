services:
  mkdocs:
    image: python:3.12-slim
    working_dir: /work
    volumes:
      - ./Rapport:/work
    ports:
      - "6001:6001"
    command: >
      sh -lc "pip install --no-cache-dir -r requirements-docs.txt
      && mkdocs serve --dev-addr 0.0.0.0:6001"
